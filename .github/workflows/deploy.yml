# Generated by /deploy-setup — starter template, review TODOs before use
name: Deploy

on:
  push:
    branches: [staging, main]

permissions:
  id-token: write
  contents: read

env:
  GCP_PROJECT_ID: paynequity
  REGION: us-central1
  SERVICE_NAME: captablebr

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Type check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      # TODO: Add your test command
      # - name: Test
      #   run: npm test

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # TODO: Configure Workload Identity Federation authentication
      # See: https://github.com/google-github-actions/auth#workload-identity-federation
      # - uses: google-github-actions/auth@ba79af03b40433c104a36f4b9926a53032c3792b # v2.1.9
      #   with:
      #     workload_identity_provider: 'projects/777685600587/locations/global/workloadIdentityPools/POOL_ID/providers/PROVIDER_ID'
      #     service_account: 'github-deployer@paynequity.iam.gserviceaccount.com'

      # TODO: Deploy to Cloud Run
      # - name: Deploy to Cloud Run
      #   uses: google-github-actions/deploy-cloudrun@e62f655d5754bec48078a72edc015367b01ee97b # v2.8.0
      #   with:
      #     service: ${{ env.SERVICE_NAME }}
      #     region: ${{ env.REGION }}
      #     project_id: ${{ env.GCP_PROJECT_ID }}
      #     source: .

      - name: Deployment placeholder
        run: echo "TODO — Configure deployment steps above"
