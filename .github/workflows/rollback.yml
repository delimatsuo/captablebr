# Generated by /deploy-setup — starter template, review TODOs before use
name: Rollback

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to rollback'
        required: true
        type: choice
        options: [staging, production]
      method:
        description: 'Rollback method'
        required: true
        type: choice
        options: [traffic-split, git-revert, redeploy-tag]
      revision_or_tag:
        description: 'Cloud Run revision or git tag'
        required: true
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # TODO: Configure authentication (same as deploy.yml)

      # TODO: Implement rollback logic
      # For Cloud Run traffic split:
      # - run: gcloud run services update-traffic captablebr --to-revisions=${{ inputs.revision_or_tag }}=100 --region=us-central1 --project=paynequity

      - name: Rollback placeholder
        run: echo "TODO — Configure rollback steps above for ${{ inputs.environment }} using ${{ inputs.method }}"
