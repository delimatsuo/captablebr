services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: captablebr
      POSTGRES_PASSWORD: captablebr_dev
      POSTGRES_DB: captablebr
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
